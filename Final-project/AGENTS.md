this is the data we have , first split it 80 10 10 or 90 10 
and then implement seasonal median + MAD as plan 1 
and then Holt Winters residual 
this is 1 week data 5 min agg 
write multiple py codes and clean at src dir 

i just want pytorch 
at last suggest some configs to run (different epoch lr etc ) at sugggestiin.md 
dont run them local 
you cant run but free to check syntaxx
use latest stable pytorch 
you are free to import libraries no pure code needs
just clean and resoruce efficienet 

you can also test some ideas like preproccessing and etc too
if you can add those labels of meta data as features is good (but just for another version ) the pure code is just based on csv
write clean compatible python codes 


dont write csv just head and tail not hard format
also for the metadata.csv that's long too
you can first try EDA to know how data is distributed on csv then config and code 
use the ml-env at the DS‌d dir DS_HW4 
pip install stuff you need for EDA‌ 
but dont install high volume GPU‌ libs as you can (if you are forced ypu can )
